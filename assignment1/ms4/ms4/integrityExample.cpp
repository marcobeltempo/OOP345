
  /*
  void OrderManager::IntegrityCheck(OrderManager& oic) {

        for (auto o : orders) {
            for (size_s count = 0; count < o.size(); count++) {
                if(oic.OrderFind(o.item(count) ) == nullptr) //refrences item but doesnt exist
                    throw string("Order ")


            }

        }

    }
  */